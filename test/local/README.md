Local-only test scripts live here.

Purpose:
- Keep personal test files separate from app code.
- Make it easy to ignore this folder in Git later.

Suggested Git ignore rule when this project is inside a Git repo:
`test/local/`
